import {
  MainTabNavigatorParams,
  NativeStackScreenProps,
} from '#/lib/routes/types';

type Props = NativeStackScreenProps<MainTabNavigatorParams, 'Home'>;
export function MainScreen(props: Props) {

}
