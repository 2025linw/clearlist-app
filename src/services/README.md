# Services (Fetch, API Calls, etc.)

## Request Schemas `#/types/request`

Request schemas are all located in the src root types folder

## Object Schemas `schemas.ts`

Contains all object schemas

## Types `types.ts`

Contains all API response types
